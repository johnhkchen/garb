services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: johnhkchen/just_rust_dev:alpha
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

